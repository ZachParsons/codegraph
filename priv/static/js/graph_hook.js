// Populated in the next slice (D3 + d3-dag rendering). Declared as an empty
// hook registry for now so app.js has something to wire the LiveSocket to.
window.CodegraphHooks = window.CodegraphHooks || {};
